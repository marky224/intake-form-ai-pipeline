# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
*.egg
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# uv: pyproject.toml and uv.lock ARE committed (deterministic builds).
# uv.lock is NOT in this ignore list intentionally.

# Environment / secrets
.env
.env.local
.env.*.local
*.pem
*.key
*.crt
secrets.json
.aws/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
*.tfbackend
!*.tfbackend.example
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
# .terraform.lock.hcl IS committed (provider version pinning).

# Node.js (web/review-ui — lands Phase 7)
node_modules/
.next/
.nuxt/
dist-ssr/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Project-specific (regenerable artifacts)
synthetic_data/output/
evals/results/

# Local model files (Phase 1 prep downloads ~66 GB of GGUFs to ~/models/qwen-vl-32b/
# outside the repo; this is belt-and-suspenders in case anyone copies one in)
*.gguf
*.safetensors

# Notebooks: commit .ipynb but ignore checkpoint dirs
.ipynb_checkpoints/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/
.claude/
CLAUDE.md

# OS
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
ehthumbs_vista.db
