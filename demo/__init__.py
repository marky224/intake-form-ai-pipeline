"""Phase 7-V1 local demo package.

``demo.data`` is the testable cascade-run + two-stage-F1 core (no Streamlit
import — CI imports and tests this). ``demo.app`` is the thin Streamlit view
layer, run only locally via ``just demo`` on ``openclaw-pc``.
"""
